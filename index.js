//import { expect } from "chai";
//import Mtrx from "mtrx";
//export const Matrix1 = new Mtrx();

/*
function Matrix(n, k) {
    const Matrix1 = new Mtrx(n,n,k);
    console.log(Matrix1)
    for (let i = 0; i < n; i++) {
        for (let j = 0; j < n; j++) {
            //console.log(Matrix1[i][j])
            if (Matrix1[i][j] != k) return false
        }
    }
    return true
}

if(Matrix(2,1)) {console.log("Basic matrix creation works")} else {console.log("Failed")}
*/